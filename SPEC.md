# SPEC.md — kokoro-graph(こころグラフ)

<!-- scaffold template v1.8.0 から展開(2026-08-17)。以後このファイルはプロジェクトが育てる -->

## 1. 目的

青空文庫の有名 300 作品(長編除外)について、行(文)単位の感情極性を開発時に前計算し、
「感情バーコードの壁」「起伏マップ散布図」「感情曲線 × 本文シンクロ」の 3 面で可視化する
静的アプリ。感情の起伏度から作品を選べる。分析は自前の版管理された感情辞書によるもので、
文学研究レベルの精度は主張せず、**作品間の相対比較と起伏の可視化に足る**ことを較正で確認する。

## 2. 機能要求

| ID | 要求 | 優先度 |
|---|---|---|
| F-01 | コーパス: 300 作品。1 作品 = 青空文庫の 1 図書カード・正規化後 200,000 字以下。`data/selection.tsv`(優先 300 + 補充プール)から索引 CSV と機械照合して確定し、除外・補充・不一致は `data/corpus_manifest.json` に全記録 | must |
| F-02 | 取得と正規化: 青空文庫からの取得(索引 CSV・本文 zip)は手動実行のみ・リクエスト間隔 ≥0.7 秒。正規化はヘッダ/フッタ(底本情報)除去・ルビ《》・ルビ起点｜・入力者注 [#…] 除去で、**除去以外の文字を一切改変しない**(逐語性)。底本情報は別途保持 | must |
| F-03 | 行分割: 文単位。規則 = 「。」「!」「?」(全角)の直後で分割し、直後の閉じ括弧(」』)は前の文に含める。段落(改行)境界は常に文境界。空文は捨てる。決定論 | must |
| F-04 | 感情辞書: 自前構築・版管理(`dict/kokoro_dict.json`、version フィールド必須)。語彙 = {語(原形), 極性 [-1,+1], カテゴリ(joy/anger/sadness/fear/like/surprise/dislike/calm)}。全スコアレコードに辞書 version を記録。辞書変更は感度分析(起伏度上位 20 の変動レポート)付きでのみ行う | must |
| F-05 | スコアリング: 行極性 = 行内の辞書ヒット語極性の平均。ヒットは fugashi(unidic-lite)の原形または表層でのマッチ。ヒット語の直後 2 形態素以内に否定(ない/ぬ/ず)があれば当該語の極性を反転。ヒット 0 の行は 0(中立)。決定論 | must |
| F-06 | 起伏度指標(1 作品ごと): 粗さ = 隣接行スコア差の平均絶対値 / 標準偏差(母集団)/ レンジ / 転調率 = 移動平均(中心窓・半径 7、端は縮小窓)曲線の符号反転回数(0 は無視)を行数で割り 100 行あたりに正規化 / 合成起伏度 = 4 指標をコーパス内 min-max 正規化した平均。定義は本表が正 | must |
| F-07 | gold 生成: `web/public/data/works/{id}.json`(行テキスト・スコア・ヒット語・カテゴリ)+ `web/public/data/index.json`(全作品のメタ・指標・64 点に縮約した曲線)。青空文庫の図書カード URL と底本表記を含む | must |
| F-08 | UI 一覧: ① 感情バーコード壁(Canvas、1 作品 = 1 本、1 縞 = 1 行、色 = 極性)② 起伏マップ散布図(x = 平均極性、y = 合成起伏度、大きさ = 行数)。並べ替え(起伏度・平均極性・作者・作品名)+ 区分フィルタ。クリックで詳細へ | must |
| F-09 | UI 詳細: 感情曲線(生スコア点 + 移動平均線)× 本文リーダーの双方向シンクロ(曲線クリック → 行へスクロール、行クリック → 曲線上で強調)。行背景を極性で着色し、辞書ヒット語をハイライト。底本・図書カードリンク表示 | must |
| F-10 | フッタ: MIT License・GitHub・操作説明・設計図・App Menu の 5 リンク | must |

## 3. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | 静的配信のみ(Next.js 15 App Router `output: "export"`)。実行時の外部通信なし(データは同一オリジンの JSON) | next build + 実機確認 |
| N-02 | 青空文庫へのアクセスは開発時の手動コマンドのみ。取得物(bronze)はコミットしない。gold(JSON)はコミットする | .gitignore + AGENTS 規約 |
| N-03 | ポインタ / タッチのみで操作完結。バーコード壁は 300 作でも初期描画が体感即時(Canvas 一括描画) | 実機確認 |
| N-04 | 純粋コア(src/core)は DOM 非依存。クライアント専用 API は useEffect 内のみ(HC-002)。非有限値は正常系として仕様化 | eslint 制約 + テスト |
| N-05 | 青空文庫の底本表記・図書カードリンクを作品詳細に表示する(慣行の尊重) | UI 確認 |

## 4. 品質基準

| ID | ゲート | 基準 |
|---|---|---|
| G-01 | 逐語性 | 正規化はフィクスチャ(実物の縮小版)で期待出力と完全一致。除去対象以外の文字列改変が 0 |
| G-02 | 分割決定論 | 分割規則のフィクスチャ(括弧・段落・連続句点の縁を含む)で完全一致 |
| G-03 | スコア厳密性 | 手書き 10 行フィクスチャ + ミニ辞書で、極性・否定反転・中立の期待値と完全一致 |
| G-04 | 起伏度厳密性 | 手計算できる小系列(≤10 点)で 5 指標すべて期待値と完全一致(浮動小数は 1e-9) |
| G-05 | コーパス完全性 | manifest の works = ちょうど 300・全て正規化後 ≤200,000 字・gold の行数と works JSON の行数が全作品で一致 |
| G-06 | 較正レポート | 起伏度上位/下位 20 と対照ペア(短調 vs 激動の既知作品)の順位表を `docs/calibration_loop00X.md` に記録し、辞書 v1 の妥当性を人間が確認できる形にする(ゲートは「レポートが生成され既知ペアの順序が崩れていないこと」) |
| G-07 | UI コア | vitest: core カバレッジ ≥90/85(fleet 標準)。verify.mjs 4 ゲート green |

## 5. スコープ外

- 実行時のオンライン形態素解析・分析(全て前計算)
- 学術辞書(PN Table 等)の同梱(ライセンス確認が要るため。参照比較は将来ループ)
- 深層学習による感情推定・文脈理解(反語・皮肉は拾えないことを明記)
- 物語型クラスタリング(k-means「物語の 6 つの型」)は将来ループ候補
- 全文検索・しおり・ユーザー保存機能
